# ============================================================================
# GLO-NCA runtime dependencies for the Docker training image. Torch is installed
# separately in the Dockerfile from the CUDA 12.1 index. Exact versions of the
# validated image; constraints-docker.txt pins the transitive set.
# ============================================================================
numpy==1.26.4            # torchio / cv2 / this codebase rely on the 1.x API
scipy==1.15.3          # HD95 distance transform + elastic-deformation aug
nibabel==5.4.2         # read .nii / .nii.gz BraTS volumes
torchio==1.2.1        # 3D intensity transforms (rescale, z-norm)
opencv-python-headless==4.11.0.86   # headless: no GUI libs needed in a container
matplotlib==3.10.9      # training curves (Agg backend, headless)
tensorboard==2.21.0    # scalar logging
pyyaml==6.0.3          # experiment YAML configs
seaborn==0.13.2        # optional NQM visualisation (imported lazily by the agent)
